schema_version = 2

[package]
kind = "skill"
slug = "sys-info"
name = "Sys Info"
description = "Retrieve system information including OS, CPU, memory, and disk usage."
version = "1.0.0"
license = "Apache-2.0"
icon = "💻"

[author]
name = "_"

[discovery]
keywords = ["system", "diagnostics", "cpu", "memory", "disk", "os"]
categories = ["utilities", "diagnostics"]

[source]
kind = "bundle"
subdir = "."

[runtime]
bins = []
env = []

[links]
repository = "https://github.com/savfox-ai/registry"
